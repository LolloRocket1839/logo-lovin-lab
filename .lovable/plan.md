## Plan

In Scene 1 of `LiquidHomepageStory.tsx`, reorder so the logo (`HeroLogo`) is the first element in the frame — above the scene index/eyebrow ("01 — Inizio / 05") and above the headline.

- Move `<HeroLogo />` to the top of Scene 1's content.
- Place the scene index row below the logo.
- Keep headline + mission paragraph order unchanged.
- No copy, brand, or other scenes touched.